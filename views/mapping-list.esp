<%@ layout "content.esp" %>

<h1>Port to Vlan Mappings</h1>
<% 
    table(0, "{ \
        columns: { \
            'port.name':    { header: 'Port' }, \
            'vlan.name':    { header: 'VLAN' }, \
            'tagged':       { header: 'Tagged' }, \
        }, \
        remote: '@mapping/edit', \
        apply: 'div.content', \
    }");
%>

<% buttonLink("New Port Mapping", 0, "{remote: '/mapping/init', apply: 'div.content'}" ); %>
